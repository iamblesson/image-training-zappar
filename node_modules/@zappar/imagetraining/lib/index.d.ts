/// <reference types="node" />
export interface TrainOptions {
    maxWidth: number;
    maxHeight: number;
    excludePreview: boolean;
}
export declare function train(fileOrData: string | Buffer, options?: Partial<TrainOptions>): Promise<Buffer>;
